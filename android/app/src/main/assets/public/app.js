const $ = (selector, root = document) => root.querySelector(selector);
const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];
const store = { get(k, f){ try { const r = localStorage.getItem(k); return r ? JSON.parse(r) : f; } catch { return f; } }, set(k,v){ localStorage.setItem(k, JSON.stringify(v)); } };
const app = { notes: store.get("neuro_notes", []), theme: store.get("neuro_theme", "light") };
function escapeHtml(v = "") { return String(v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;"); }
function formatDate(ts){ try { return new Date(ts).toLocaleString(); } catch { return ""; } }
function syncStats(){ const s = $("#statsText"); const d = $("#dashNotesCount"); if (s) s.textContent = `${app.notes.length} notes saved`; if (d) d.textContent = app.notes.length; }
function saveNotes(){ store.set("neuro_notes", app.notes); syncStats(); }
function resetNoteForm(){ ["#noteTitle","#noteBody","#editingNoteId"].forEach(id=>{ const el=$(id); if (el) el.value=""; }); }
function fillNoteForm(note){ const t=$("#noteTitle"), b=$("#noteBody"), e=$("#editingNoteId"); if (t) t.value=note.title||""; if (b) b.value=note.body||""; if (e) e.value=note.id||""; window.scrollTo({ top: 0, behavior: "smooth" }); }
function deleteNote(id){ app.notes = app.notes.filter(n => n.id !== id); saveNotes(); renderNotes(); }
function renderNotes(){ const list=$("#notesList"); const search=$("#searchNotes"); if (!list) return; const q=(search?.value||"").trim().toLowerCase(); const filtered = app.notes.filter(n => (`${n.title||""} ${n.body||""}`).toLowerCase().includes(q)); if (!filtered.length){ list.innerHTML = `<article class="note-card"><h4>No notes found</h4><p>Create your first note or change the search text.</p></article>`; return; } list.innerHTML = filtered.map(note => `<article class="note-card"><h4>${escapeHtml(note.title)}</h4><div class="note-meta">Updated ${escapeHtml(formatDate(note.updatedAt))}</div><p>${escapeHtml(note.body)}</p><div class="note-actions"><button class="neu-btn edit-note" data-id="${note.id}" type="button">Edit</button><button class="neu-btn delete-note" data-id="${note.id}" type="button">Delete</button></div></article>`).join(""); $$(".edit-note").forEach(btn=>btn.addEventListener("click",()=>{ const note = app.notes.find(item => item.id === btn.dataset.id); if (note) fillNoteForm(note); })); $$(".delete-note").forEach(btn=>btn.addEventListener("click",()=>deleteNote(btn.dataset.id))); }
function exportNotes(){ const blob = new Blob([JSON.stringify(app.notes, null, 2)], { type: "application/json" }); const url = URL.createObjectURL(blob); const a = document.createElement("a"); a.href = url; a.download = "neurostack-notes.json"; a.click(); URL.revokeObjectURL(url); }
function buildPrompt(){ const role=$("#pbRole")?.value.trim()||"", goal=$("#pbGoal")?.value.trim()||"", audience=$("#pbAudience")?.value.trim()||"", tone=$("#pbTone")?.value.trim()||"", context=$("#pbContext")?.value.trim()||"", constraints=$("#pbConstraints")?.value.trim()||"", format=$("#pbFormat")?.value.trim()||""; const lines=[ role?`You are ${role}.`:"", goal?`Primary goal: ${goal}.`:"", audience?`Target audience: ${audience}.`:"", tone?`Tone: ${tone}.`:"", context?`Context: ${context}`:"", constraints?`Constraints: ${constraints}`:"", format?`Output format: ${format}`:"", "Respond clearly, specifically, and with practical detail." ].filter(Boolean); const out=$("#promptOutput"); if (out) out.value = lines.join("
"); }
function clearPromptBuilder(){ ["#pbRole","#pbGoal","#pbAudience","#pbTone","#pbContext","#pbConstraints","#pbFormat","#promptOutput"].forEach(id=>{ const el=$(id); if (el) el.value=""; }); }
function applyTheme(theme){ document.documentElement.setAttribute("data-theme", theme); app.theme = theme; store.set("neuro_theme", theme); const t=$("#themeToggle"); if (t) t.textContent = theme === "dark" ? "☀️ Light" : "🌙 Dark"; }
function toggleTheme(){ applyTheme(app.theme === "dark" ? "light" : "dark"); }
function clearAllData(){ localStorage.removeItem("neuro_notes"); localStorage.removeItem("gemini_settings"); localStorage.removeItem("neuro_theme"); app.notes = []; syncStats(); renderNotes(); resetNoteForm(); applyTheme("light"); }
function bindCommon(){ $("#resetNote")?.addEventListener("click", resetNoteForm); $("#searchNotes")?.addEventListener("input", renderNotes); $("#exportNotes")?.addEventListener("click", exportNotes); $("#buildPrompt")?.addEventListener("click", buildPrompt); $("#clearPrompt")?.addEventListener("click", clearPromptBuilder); $("#copyPrompt")?.addEventListener("click", async ()=>{ const text = $("#promptOutput")?.value || ""; if (!text) return; try { await navigator.clipboard.writeText(text); } catch {} }); $("#clearAllData")?.addEventListener("click", clearAllData); $("#themeToggle")?.addEventListener("click", toggleTheme); const form=$("#noteForm"); if (form) { form.addEventListener("submit", e=>{ e.preventDefault(); const title=$("#noteTitle")?.value.trim()||""; const body=$("#noteBody")?.value.trim()||""; const editingId=$("#editingNoteId")?.value||""; if (!title || !body) return; if (editingId) { app.notes = app.notes.map(n => n.id === editingId ? { ...n, title, body, updatedAt: Date.now() } : n); } else { app.notes.unshift({ id: (window.crypto && crypto.randomUUID) ? crypto.randomUUID() : String(Date.now()), title, body, updatedAt: Date.now() }); } saveNotes(); resetNoteForm(); renderNotes(); }); } }
document.addEventListener("DOMContentLoaded", ()=>{ applyTheme(app.theme || "light"); syncStats(); renderNotes(); bindCommon(); });
